name: Publish to npm

on:
  workflow_dispatch:
    inputs:
      tag:
        description: npm dist-tag to publish under
        required: true
        default: latest
        type: string
      dry_run:
        description: Run the publish step in dry-run mode
        required: true
        default: false
        type: boolean

jobs:
  publish:
    if: github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write

    steps:
      - name: Check out repository
        uses: actions/checkout@v6

      - name: Set up Vite+
        uses: voidzero-dev/setup-vp@v1
        with:
          node-version: '24'
          cache: true
          run-install: false

      - name: Install dependencies
        run: vp install

      - name: Run checks
        run: vp check

      - name: Pack package
        run: vp pack

      - name: Run tests
        run: vp test

      - name: Publish package
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
          NPM_CONFIG_PROVENANCE: true
        run: |
          publish_args=(
            .
            --access public
            --tag "${{ inputs.tag }}"
            --publish-branch main
            --no-git-checks
          )

          if [ "${{ inputs.dry_run }}" = 'true' ]; then
            publish_args+=(--dry-run)
          fi

          vp pm publish "${publish_args[@]}"
